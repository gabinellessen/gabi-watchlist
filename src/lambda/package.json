{
  "name": "watchlist-report",
  "version": "1.0.0",
  "description": "Lambda /report — consome a API Gateway e retorna estatísticas em JSON",
  "type": "module",
  "main": "index.mjs",
  "private": true,
  "scripts": {
    "local": "node -e \"import('./index.mjs').then(m => m.handler({}).then(r => console.log(r.body)))\""
  }
}
